<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fishing.backend.mapper.UserAccountMapper">

  <resultMap id="UserAccountMap" type="com.fishing.backend.domain.UserAccount">
    <id property="id" column="id" />
    <result property="email" column="email" />
    <result property="passwordHash" column="password_hash" />
    <result property="name" column="name" />
    <result property="phone" column="phone" />
    <result property="role" column="role" />
    <result property="createdAt" column="created_at" />
    <result property="updatedAt" column="updated_at" />
  </resultMap>

  <select id="findByEmail" resultMap="UserAccountMap">
    SELECT id, email, password_hash, name, phone, role, created_at, updated_at
    FROM user_account
    WHERE email = #{email}
  </select>

  <select id="findById" resultMap="UserAccountMap">
    SELECT id, email, password_hash, name, phone, role, created_at, updated_at
    FROM user_account
    WHERE id = #{id}
  </select>

  <insert id="upsertDevUser">
    INSERT INTO user_account (email, password_hash, name, phone, role)
    VALUES (#{email}, #{passwordHash}, #{name}, #{phone}, #{role})
    ON DUPLICATE KEY UPDATE
      password_hash = VALUES(password_hash),
      name = VALUES(name),
      phone = VALUES(phone),
      role = VALUES(role)
  </insert>

</mapper>

